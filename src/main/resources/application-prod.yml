spring:
  application:
    name: mml
  datasource:
    url: jdbc:mysql://localhost:3306/mml
    username: root
    password: jdg1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect